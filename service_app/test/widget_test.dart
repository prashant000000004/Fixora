import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App renders', (WidgetTester tester) async {
    // Placeholder test — real widget tests to be added per feature.
  });
}
